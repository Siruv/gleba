# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# PROMPT 16 LOT A — uploads utilisateurs (photos produits, observations, arbres).
# Le dossier doit exister dans l'image Docker mais ses contenus ne doivent pas
# être commités (données user-specific).
/public/uploads/*
!/public/uploads/.gitkeep
/storage/

# next.js
/.next/
/out/

# production
/build

# Android / Google Play
# Clé d'upload et mot de passe privés : ne jamais les versionner.
/.android-signing/
/android/gleba-twa/.gradle/
/android/gleba-twa/build/
/android/gleba-twa/app/build/
/android/gleba-twa/assetlinks.json
/android/gleba-twa/app-release-*.apk
/android/gleba-twa/app-release-*.aab
/android/gleba-twa/*.idsig
/dist/android/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
# Motif large VOLONTAIREMENT : `.env` et `.env*.local` seuls laissaient passer
# `.env.bak-*`, `.env.save`, `.env.production`… c'est-à-dire des fichiers de
# SECRETS dans un dépôt public. Constaté le 2026-08-19 avec une sauvegarde de
# `.env` prise avant une bascule, qu'un `git add -A` aurait publiée.
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# Development notes (not for production)
AGENT_PROMPT.md
ANALYSIS.md
CLAUDE.md
CLAUDE.md.*

# Confidential
dl/
PROMPT_AI_ENRICHMENT.md

# Junk: artefacts de redirection shell ratés (ex: `> et)` au lieu de `| cat`)
et)

# AI / MCP
# Le chat IA et les endpoints MCP sont open source (Phase 6).
/mcp-server/

# Boutique en ligne (proprietary)
# Modèles Prisma laissés publics (la compta y fait référence), mais
# l'UI / l'API / les composants UI restent privés.
/src/app/boutique/
/src/components/boutique/
/src/app/api/boutique/

# Système de feedback (proprietary)
# Outil opérationnel propre à gleba.fr (campagnes emails, enquêtes de
# satisfaction, signalement de bugs). Non distribué aux instances
# auto-hébergées. Les modèles Prisma (BugReport, FeedbackToken,
# FeedbackResponse) et le schéma de validation restent publics ; seuls
# l'UI, les routes, les scripts de campagne et les templates d'emails sont
# privés. Le composant FeedbackWidget garde un STUB public (cf.
# docs/private-stubs.md), le vrai code reste sur le VPS via skip-worktree.
/src/app/feedback/
/src/app/admin/feedback/
/src/app/api/feedback/
/src/app/api/admin/feedback/
/src/lib/mail-feedback.ts
/scripts/send-feedback-campaign.ts
/scripts/send-feedback-invite.ts
/scripts/generate-feedback-tokens.ts
/scripts/send-feedback-segmented.ts
/scripts/send-merci-gps.ts
/scripts/growth-campaign.ts
/docs/implementation-artifacts/tech-spec-feedback-widget.md

# Artefacts d'outils de revue (non versionnés)
/codex-review.zip
/codex-review-diff.zip
/CODEX-DIFF-REVIEW-LOG.md
/.paperclip/

# Tooling agent local (cohérent avec CLAUDE.md ci-dessus)
/AGENTS.md
/.claude/skills/codex-review/
/.claude/skills/codex-review-diff/

# La consultation admin fait partie du produit : ses sources doivent rester
# versionnées avec auth.ts/layout.tsx afin qu'un clone propre compile.

# ─────────────────────────────────────────────────────────────────────────────
# Scripts one-shot porteurs de DONNÉES PERSONNELLES réelles (dépôt public).
# Ils ont servi une fois, contre la base de production, et contiennent des
# adresses email, des noms et des questions d'utilisateurs identifiables :
# rien de tout cela ne doit entrer dans l'historique Git, qui est indélébile.
# Les garder en local est utile ; les publier serait une violation RGPD.
# ─────────────────────────────────────────────────────────────────────────────
/scripts/debloquer-jeanjacques-20260811.ts
/scripts/reponse-vorwerk-fleurs-20260818.ts
/scripts/replay-questions-reelles.ts

# Rebut de travail (sauvegardes d'éditeur, page 3D retirée du site)
/.dl-retire/
*.bak-vc3

# ─────────────────────────────────────────────────────────────────────────────
# Outillage d'agent : reste sur le VPS, jamais dans le dépôt public.
# Complète l'intention déjà déclarée plus haut, qui n'excluait que 3 chemins.
# ─────────────────────────────────────────────────────────────────────────────
/_bmad/
/.claude/

# Artefacts générés (régénérables, bruit de diff)
/docs/project-scan-report.json
/docs/source-tree-analysis.md

# Support commercial et visuels de travail : gardés sur la machine, hors dépôt.
# Le deck expose le modèle tarifaire, il n'a rien à faire dans le code ; l'image
# générée pesait 6,5 Mo dans chaque clone sans être référencée nulle part.
/gleba-pro.pptx
/Gemini_Generated_Image_*.png
